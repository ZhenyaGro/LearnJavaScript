/*
Rewrite the if construct using the '?' conditional operator:

let result;

if (a + b < 4) {
  result = 'Low';
} else {
  result = 'High';
}
*/

"use strict";

result = (a + b < 4) ? 'Low' : 'High';